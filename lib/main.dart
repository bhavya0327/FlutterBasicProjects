import 'package:flutter/material.dart';

import 'ui/home.dart';

// void main() => runApp(const ScaffoldExample());


// You just have to change the name after home: ..... to the name of the app you want to run
void main() => runApp(new MaterialApp(
  home: QuotesDaily(),                  //if home: BizCard(), then BusinessCard App will run
));
